import type { CSSProperties, SVGProps } from 'react';
import type { ExtendedDataEntry } from './commonTypes';
export declare type LabelRenderProps = {
    x: number;
    y: number;
    dx: number;
    dy: number;
    textAnchor: string;
    dataEntry: ExtendedDataEntry;
    dataIndex: number;
    style?: CSSProperties;
};
declare type Props = SVGProps<SVGTextElement> & LabelRenderProps;
declare function Label({ dataEntry, dataIndex, ...props }: Props): JSX.Element;
declare namespace Label {
    var displayName: string;
}
export default Label;
